# Backend Environment Variables

# === HYBRID AUTHENTICATION (Clerk + JWT) ===

# Clerk Authentication (for user authentication)
# Get these from https://dashboard.clerk.com -> API Keys -> Show JWT public key
CLERK_PEM_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----\nYour_Clerk_Public_Key_Here\n-----END PUBLIC KEY-----
CLERK_ISSUER=https://your-clerk-domain.clerk.accounts.dev

# JWT Authentication (for API-to-API or legacy systems)
JWT_SECRET=your_secret_key_change_in_production_use_openssl_rand_hex_32
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# API Key (for generating JWT tokens)
API_KEY=your_api_key_for_jwt_generation

# MongoDB Configuration
MONGODB_URL=mongodb://localhost:27017
MONGODB_DB_NAME=whatsapp_business

# WhatsApp Business API
WHATSAPP_API_URL=https://graph.facebook.com/v18.0
WHATSAPP_ACCESS_TOKEN=your_whatsapp_token_here
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id

# Google Sheets API
GOOGLE_SHEETS_CREDENTIALS_FILE=credentials.json

# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=True

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002
